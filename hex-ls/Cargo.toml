[package]
name = "hex-ls"
version = "0.3.0"
edition = "2021"

[[bin]]
name = "hex-ls"
path = "src/main.rs"

[dependencies]
lsp-server = "0.7"
lsp-types = "0.97"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "2.9", features = ["json"] }
regex = "1"
